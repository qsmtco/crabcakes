[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    slow: long-running measurement rows (excluded from fast suites)
    manual: needs the live app or a human-run procedure — not CI-automatable
