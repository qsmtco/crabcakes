# Enter Worktree Tool

Create an isolated git worktree and switch the current session into it.

## When to Use

Only when the user explicitly asks to work in a worktree. This provides an isolated copy of the repository.

## What It Does

- Creates a new git worktree
- Switches the session into the worktree
- Isolates changes from the main branch
