TASK FOR QTR — Step 1: Add agent_builder_factory to wire_settings_handler
=========================================================================

TRIGGER WORD: proceed

CONTEXT
-------
This is Step 1 of the spec at `docs/specs/SPEC-AGENT-BUILDER-PROVIDER-DROPDOWN.md`.
Do exactly Step 1 only. Do not touch other files. Do not implement later steps.

READ THESE FILES IN FULL:
  1. The prompt: `prompts/steelFramedCodeWriter.md`
  2. The spec: `docs/specs/SPEC-AGENT-BUILDER-PROVIDER-DROPDOWN.md` (§2.3 specifically)
  3. The current file: `ui/wiring.py`
  4. The current callers: `ui/window.py` (lines 225-232)
  5. The current tests: `tests/test_window_settings_wiring.py`

Follow the steelFramedCodeWriter prompt EXACTLY with no deviation.

STEP 1 — WHAT TO DO
-------------------
Add an optional `agent_builder_factory: Callable | None = None` parameter to
`wire_settings_handler()` in `ui/wiring.py`.

In the `_on_providers_changed` callback inside `wire_settings_handler`, after
the existing `settings_dialog_factory()` block, add a new block that calls
`agent_builder_factory().set_provider_options(providers)` when the factory
returns a non-None dialog. Mirror the same try/except + logger.warning pattern
as the settings dialog block.

Both blocks are no-ops if their factory returns None.

DO NOT
------
- Do not touch `ui/window.py` — that's Step 2.
- Do not touch `ui/views/agent_builder.py` — that's Steps 3-5.
- Do not touch any test file — tests are Step 7.
- Do not change the existing `settings_dialog_factory` behavior.
- Do not change the public API of any other function.

VERIFICATION
------------
Run these and confirm all pass:

  1. python3 -m pytest tests/test_window_settings_wiring.py -v --tb=short
     Must show all existing tests still pass.
  2. python3 -m pytest tests/test_settings_handler.py -q
     Must show all existing tests still pass.
  3. grep -n "agent_builder_factory" ui/wiring.py
     Must show the parameter in the signature AND the call site in
     _on_providers_changed.
  4. python3 -c "from ui.wiring import wire_settings_handler; import inspect; print(inspect.signature(wire_settings_handler))"
     Must show `agent_builder_factory` in the signature.

WHEN DONE
---------
Reply: "Step 1 complete — agent_builder_factory added to wire_settings_handler."
